# API server (Express on port 5000). Leave empty in dev to use Vite proxy (/api → localhost:5000).
VITE_API_URL=http://localhost:5000

# Must match server ADMIN_CLIENT_URL / CLIENT_URL for CORS
# VITE_DEV_ORIGIN=http://localhost:5173
